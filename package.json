{
  "name": "medaware",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"pnpm --filter api run dev\" \"wait-on -t 60000 http://localhost:3001 && pnpm --filter web run dev\"",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "db:seed": "pnpm --filter api exec prisma db seed",
    "db:migrate": "pnpm --filter api exec prisma migrate deploy"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "turbo": "^2.3.0",
    "wait-on": "^9.0.4"
  },
  "packageManager": "pnpm@9.14.2",
  "engines": {
    "node": ">=18"
  }
}
