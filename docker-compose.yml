services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: medaware
      POSTGRES_PASSWORD: medaware
      POSTGRES_DB: medaware
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
